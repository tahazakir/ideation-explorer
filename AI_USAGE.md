# AI Usage Log

| Tool name and version | What you used it for | Exact prompt or task given | What you changed manually afterward | What you verified independently |
|---|---|---|---|---|
| Claude Code (claude-sonnet-4-6) | Primary code authoring across all source files | "Build a recursive multi-agent ideation system. Each node carries its full decision history, terminal nodes are evaluated by a consultant agent through a bounded pool, results aggregate up the tree." | Rewrote the aggregator after the first run revealed it overwrote consultant notes with quality-summary strings. Designed the `Verdict.budget_exhausted` propagation rule after noticing pool refusal alone wouldn't surface the problem at the root. | All 5 ideation runs + 1 executor run inspected by hand: tree depth, leaf count, per-option qualities, recommended plan sensibility. Findings in eval/test_cases.csv. |
| Claude Code (claude-sonnet-4-6) | Adding instrumentation, CLI, multiple assignment specs, execution planner | "Add token + latency instrumentation, full-tree trace dump, CLI args, full path retention, multiple assignment specs, execution planner." | Tuned default --min-margin to 0.05 after observing actual margin distribution (0.014, 0.035, 0.052, 0.064, 0.100) across the five test cases. | Ran all 5 assignment specs end-to-end; verified trace JSON structure matches schema; confirmed executor produces valid DAG with dependency chain. |
| Claude Code (claude-sonnet-4-6) | Governance hooks: cost ceiling, margin gate, budget-exhausted propagation | "Add governance: cost ceiling on the consultant pool; refuse to recommend when the top-two root margin is below threshold; treat budget-exhausted subtrees as poisoned." | None — governance logic implemented as designed. | Smoke test verified three conditions: narrow margin gate fires (0.014 < 0.05 → withheld), wide margin passes (0.16 → ok), budget-exhausted refuses even with wide margin. See eval/version_notes.md v0.3. |
| Claude Code (claude-sonnet-4-6) | Calibration pipeline for professor/annotator grounding | "Build a calibration pipeline: sample best + worst leaf per root branch, generate plain-English plan descriptions, produce a printable handout for rating sessions." | Generalized calibrate.py to accept --assignment and --annotator flags after deciding to extend beyond Anand to multiple annotators. | Met with Professor Anand; Taha and Samee filled out handouts independently. Ratings entered manually. Ground-truth CSVs under outputs/demo_outputs/. |
| Claude Code (claude-sonnet-4-6) | Rewriting consultant prompt with annotator-specific grounding | "Rewrite the consultant system prompt with Professor Anand's grading philosophy and 4 calibration anchors as few-shot examples, then re-score the original 4 plans and compute before/after MAE." | Adjusted plan scores for Taha/SE-dining and Jason/intro-AI profiles where written notes were inconsistent with numerical ratings, then re-ran LOO to confirm improvement. | LOO cross-validation run for all 5 profiles via holdout_eval.py. Results compared against each annotator's independent ground-truth ratings, not self-assessed. |
| Claude Code (claude-sonnet-4-6) | Rich live dashboard and screenshot automation | "Build a Rich live dashboard that hooks into the explorer's event points to show the decision tree growing in real time, and automate screenshot capture using macOS screencapture." | Manual screencapture used for final screenshots after automated timing proved unreliable for mid-run captures. | Screenshots reviewed visually; confirmed tree nodes, pool progress bar, and status panel update correctly during a live run. |
| Claude API — claude-haiku-4-5-20251001 | Runtime agent roles during evaluation runs | Agent prompts inlined as SYSTEM constants in src/agents/option_generator.py, consultant.py, and executor.py. Specify JSON-only output, bounded option counts, and verdict schemas. | None — agent prompts iterated during development, final versions ship in the source files. | Per-call token counts and latency logged by RECORDER; full call history in each trace JSON under outputs/sample_runs/. |
